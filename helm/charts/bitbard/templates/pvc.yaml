apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "bitbard.name" . }}
  labels:
  {{- include "bitbard.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.capacity }}
