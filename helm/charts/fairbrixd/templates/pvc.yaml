apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "fairbrixd.name" . }}
  labels:
  {{- include "fairbrixd.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.capacity }}
