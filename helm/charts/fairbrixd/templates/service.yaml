apiVersion: v1
kind: Service
metadata:
  name: fairbrix
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "fairbrixd.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "fairbrixd.selectorLabels" . | nindent 4 }}
  type: {{ .Values.service.type }}

  ports:
    {{- range $.Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol | quote }}
      name: {{ .name | quote }}
    {{- end }}
