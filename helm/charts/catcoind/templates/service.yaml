apiVersion: v1
kind: Service
metadata:
  name: catcoin
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "catcoind.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "catcoind.selectorLabels" . | nindent 4 }}
  type: {{ .Values.service.type }}

  ports:
    {{- range $.Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol | quote }}
      name: {{ .name | quote }}
    {{- end }}
