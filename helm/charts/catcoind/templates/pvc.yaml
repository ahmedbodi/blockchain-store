apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "catcoind.name" . }}
  labels:
  {{- include "catcoind.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.capacity }}
