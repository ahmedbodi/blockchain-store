apiVersion: v1
kind: Service
metadata:
  name: meowcoin
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "meowcoind.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "meowcoind.selectorLabels" . | nindent 4 }}
  type: {{ .Values.service.type }}

  ports:
    {{- range $.Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol | quote }}
      name: {{ .name | quote }}
    {{- end }}
