apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "meowcoind.name" . }}
  labels:
  {{- include "meowcoind.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.capacity }}
